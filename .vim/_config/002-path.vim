"VIMCONFIG
if has('nvim')
  let $DOTVIM='~/.config/nvim/'
else
  let $DOTVIM='~/.vim/'
endif

if has('nvim')
  let $VIMCONF='~/.config/nvim/_config/'
else
  let $VIMCONF='~/.vim/_config/'
endif

let $DESKTOP='~/Desktop'
