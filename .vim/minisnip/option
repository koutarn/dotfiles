CMD_NAME=$(basename $0)

function print_usage {
    cat <<EOF
    $CMD_NAME is a tool for ...

  Usage:
    $CMD_NAME [command] [<options>]

  Options:
    --version, -v     $CMD_NAME version
    --help, -h        help page
EOF
}

function print_version {
  echo "$CMD_NAME version 1.0"
}
