[Leader]
space

[Prefix]
データ操作(汎用) <leader> + f
    QuickFix q
    LocationList l
    fern f
    vista v
    spectre s
    floaterm t
    設定の再読み込み r
    vimrcの編集 ,
    cheatsheetの編集 .
    バックアップの表示(aho-backup) b
    便利ヘルプ表示 h

Bookmark <Leader> + m
Gina <Leader> + g
Debug(nvim-dap) <Leader> + d
coc <Leader> + ;
cheatsheet表示 ?

[Operator]
| Operator Name        | Bind | 効果     |
| -------------------- | ---- | -------- |
| vim-fperator-replace | R    | 置換     |
| vim-sandwich         | sa   | 囲み追加 |
| vim-sandwich         | sr   | 囲み置換 |
| vim-sandwich         | sd   | 囲み削除 |

[Text Object]
| Object Name               | Bind    | 範囲                           |
| -------------------       | ------- | ------------------------------ |
| vim-textobj-entire        | e       | バッファ                       |
| vim-textobj-comment       | c       | コメント                       |
| vim-textobj-function      | F       | 関数の内部                     |
| vim-textobj-functioncall  | C       | 関数の呼びだし部分             |
| vim-textobj-parameter     | ,       | 関数の引数                     |
| vim-textobj-indent        | i       | インデント                     |
| vim-textobj-line'         | l       | 行                             |
| vim-textobj-anyblock      | b       | 複数ブロック対応               |
| vim-textobj-between       | f{char} | 任意の{char}で囲まれている文字 |
| vim-textobj-url           | u       | URL                            |

[さがしもの]
cmdline-special コマンドライン用の特別な文字

[TODO]
;qa
qa
タブについてもっと有効活用出来るようにする。
barbarのタブについて設定する。
nvim-dapでlaunch.jsonの使いかたを調べる。
quickrunのヘルプを読んで使いかたを理解する
lightlineからgalaxylineかlualineに変更する。
fzf-previewのgrepでプレビューが効かないのを直す。
@:と@@を押しやすい箇所にバインドする
windows(職場)対応を厳格にする
    pluginの読み込みを早める(dein導入?)
    eskkを入れる。

ここらへんを試す。
https://github.com/Shougo/dein.vim
https://github.com/NTBBloodbath/doom-one.nvim
https://github.com/mhinz/vim-startify

参考にしていくリポジトリ
https://github.com/yutkat/dotfiles/tree/master/.config/nvim
https://github.com/monaqa/dotfiles/tree/master/.config/nvim

[作りたい]
* バッファを簡単に管理出来る機能(floating window)
* vim-operator-useでひらがな、カタカナ、半角カタカナをトグルするやつ
* vim-operator-useでeskkで再度変換をかけれるようにする。 vim-operator-saihenkan
    * 渡された文字列が日本語かを正規表現で判定する。
    * ひらがなに変換する。
    * 変換待機モードに入る。 
